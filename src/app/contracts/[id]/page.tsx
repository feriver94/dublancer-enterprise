import { AuthenticatedShell,Container } from "@/components/layout";import ContractDetailClient from "@/components/contracts/ContractDetailClient";export default async function ContractDetailPage({params}:{params:Promise<{id:string}>}){const{id}=await params;return <AuthenticatedShell returnTo={`/contracts/${id}`}><Container><ContractDetailClient contractId={id}/></Container></AuthenticatedShell>}
