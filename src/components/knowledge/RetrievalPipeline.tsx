import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const steps=[["Ingest","Upload, connect, or sync trusted sources"],["Classify","Detect owner, sensitivity, permissions"],["Chunk","Split into retrieval-safe windows"],["Embed","Generate vector representations"],["Rank","Apply hybrid retrieval and freshness scoring"],["Answer","Ground AI responses with source-aware context"]];export default function RetrievalPipeline(){return <Card variant="elevated"><Badge variant="success">RAG Pipeline</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Retrieval architecture</h2><div style={{display:"grid",gap:12}}>{steps.map(([s,d],i)=><div key={s} style={{display:"grid",gridTemplateColumns:"54px 120px 1fr",gap:14,alignItems:"center",padding:14,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><Badge variant="neutral">{String(i+1).padStart(2,"0")}</Badge><strong style={{color:brand.colors.green}}>{s}</strong><span style={{color:brand.colors.text,lineHeight:1.6}}>{d}</span></div>)}</div></Card>}