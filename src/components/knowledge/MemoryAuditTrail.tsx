import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const events=["Client profile memory updated by CRM Agent.","Workspace decision indexed from project room.","PII redaction applied to uploaded contract.","Finance memory access approved by Owner role.","Agent run linked to source documents and audit event.","Knowledge export prepared for compliance review."];export default function MemoryAuditTrail(){return <Card variant="elevated"><Badge variant="info">Memory Audit</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Traceable memory events</h2><div style={{display:"grid",gap:12}}>{events.map(e=><div key={e} style={{padding:"14px 0",borderBottom:`1px solid ${brand.colors.border}`,color:brand.colors.text,lineHeight:1.6}}>{e}</div>)}</div></Card>}