import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";

export default function ConnectorRuntimeHeader(){return <Card variant="glass" style={{padding:44,marginBottom:28}}><Badge variant="success">Connector Runtime, OAuth, Webhook & Sync Platform</Badge><h1 style={{color:brand.colors.navy,fontSize:"clamp(2.8rem,6vw,5.7rem)",fontWeight:brand.typography.weight.bold,lineHeight:.94,margin:"22px 0"}}>Run every connector with secure authorization, durable synchronization, and observable execution.</h1><p style={{color:brand.colors.muted,fontSize:brand.typography.body.lg,lineHeight:1.8,maxWidth:940}}>A production integration runtime for OAuth installations, scoped credentials, signed webhooks, incremental sync, connector actions, retries, rate limits, schema mapping, sandbox testing, and tenant-safe execution.</p><div style={{display:"flex",gap:12,flexWrap:"wrap",marginTop:24}}><Button variant="primary">Create Connector Run</Button><Button variant="outline">Start OAuth Install</Button><Button variant="ghost">Replay Webhook</Button></div></Card>}
