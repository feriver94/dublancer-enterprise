import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const insights=["Persist workflow definitions as versioned schemas, never only UI state.","Every execution needs idempotency, retries, compensation, audit lineage, and tenant isolation.","High-risk actions must route through approval nodes.","Create an event schema registry before adding more integrations.","Production execution requires backend workers, queues, and database-backed run state."];export default function WorkflowEngineArchitect(){return <Card variant="glass"><Badge variant="success">Workflow Engine Architect</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18}}>Production orchestration recommendations</h2><div style={{display:"grid",gap:12,marginTop:20}}>{insights.map(i=><div key={i} style={{padding:14,border:`1px solid ${brand.colors.border}`,borderRadius:brand.radius.md}}>{i}</div>)}</div></Card>}