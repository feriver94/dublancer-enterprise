import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const insights=["Agent runtime should be implemented as a backend worker service, not only a UI screen.","Every agent run must store prompt, model, tool calls, retrieved memory, output, cost, latency, approval status, and audit lineage.","Tool permissions must reuse Identity Platform RBAC so agents cannot bypass user access boundaries.","Model routing should optimize for quality, cost, privacy, fallback reliability, and enterprise tenant policy.","Next implementation step: create agent_runs, agent_tools, agent_memory_refs, model_routes, and ai_audit_events tables in Prisma."];export default function AIRuntimeAI(){return <Card variant="glass" style={{background:"linear-gradient(135deg,rgba(255,255,255,.96),rgba(248,250,252,.82))"}}><Badge variant="success">AI Runtime Architect</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:12}}>Production AI execution recommendations</h2><p style={{color:brand.colors.muted,lineHeight:1.7,marginBottom:22}}>AI reviews runtime design, tool boundaries, memory access, cost, observability, and production readiness.</p><div style={{display:"grid",gap:12}}>{insights.map(i=><div key={i} style={{padding:14,borderRadius:brand.radius.md,background:brand.colors.white,border:`1px solid ${brand.colors.border}`,color:brand.colors.text,lineHeight:1.6}}>{i}</div>)}</div></Card>}