import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";

const insights=["Every connector should declare scopes, data classes, rate limits, events, retry behavior, and tenant boundaries.","OAuth tokens and API credentials must remain server-side, encrypted, rotated, revocable, and unavailable to client bundles.","Connector actions should run through the workflow engine with idempotency, retries, audit lineage, and dead-letter handling.","Finance, identity, and external-sharing connectors should require admin approval and continuous monitoring.","Production requires provider applications, OAuth callbacks, encrypted credential storage, queue-backed sync workers, and connector-specific tests."];
export default function IntegrationsAI(){return <Card variant="glass"><Badge variant="success">Integration Marketplace AI</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18}}>Connector architecture recommendations</h2><div style={{display:"grid",gap:12,marginTop:20}}>{insights.map(i=><div key={i} style={{padding:14,border:`1px solid ${brand.colors.border}`,borderRadius:brand.radius.md}}>{i}</div>)}</div></Card>}
