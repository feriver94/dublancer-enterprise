import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";

export default function MessagingRuntimeHeader(){return <Card variant="glass" style={{padding:44,marginBottom:28}}><Badge variant="success">Omnichannel Messaging & Notification Runtime</Badge><h1 style={{color:brand.colors.navy,fontSize:"clamp(2.8rem,6vw,5.7rem)",fontWeight:brand.typography.weight.bold,lineHeight:.94,margin:"22px 0"}}>Deliver every message through the right channel, at the right time, with enterprise-grade control.</h1><p style={{color:brand.colors.muted,fontSize:brand.typography.body.lg,lineHeight:1.8,maxWidth:940}}>A resilient delivery layer for email, SMS, push, in-app, chat, webhooks, preferences, localization, templates, routing, retries, compliance, and observability.</p><div style={{display:"flex",gap:12,flexWrap:"wrap",marginTop:24}}><Button variant="primary">Send Test Message</Button><Button variant="outline">Create Template</Button><Button variant="ghost">Review Delivery Health</Button></div></Card>}
