import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const env=[["Seed Data","Demo clients, invoices, projects, agents","Ready"],["Mock Payments","Stripe-style test transactions","Ready"],["Webhook Tester","Replay and inspect delivery attempts","Ready"],["API Console","Run authenticated test requests","Ready"],["Error Simulator","Trigger retry and failure states","Ready"]];export default function SandboxEnvironment(){return <Card variant="elevated"><Badge variant="info">Sandbox</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Safe developer playground</h2><div style={{display:"grid",gap:12}}>{env.map(([n,d,s])=><div key={n} style={{display:"flex",justifyContent:"space-between",gap:16,padding:14,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><div><strong style={{color:brand.colors.navy}}>{n}</strong><p style={{color:brand.colors.muted,margin:"6px 0 0"}}>{d}</p></div><Badge variant="success">{s}</Badge></div>)}</div></Card>}