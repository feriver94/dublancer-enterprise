import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const logs=["API key created by Organization Owner.","Webhook delivery succeeded for invoice.updated.","Sandbox request generated a mock payment event.","Partner app exceeded warning threshold at 80% quota.","OpenAPI schema exported for production integration.","Webhook signing secret rotated successfully."];export default function DeveloperLogs(){return <Card variant="elevated"><Badge variant="neutral">Developer Logs</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Audit-ready developer activity</h2><div style={{display:"grid",gap:12}}>{logs.map(log=><div key={log} style={{padding:"14px 0",borderBottom:`1px solid ${brand.colors.border}`,color:brand.colors.text,lineHeight:1.6}}>{log}</div>)}</div></Card>}