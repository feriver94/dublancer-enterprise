import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const rows=[['Expansion path detected', 'Horizon usage connects to agent automation', 'Strong'], ['Retention risk cluster', 'Nexa Cloud payment and support signals', 'Risk'], ['Automation opportunity', 'Proposal approvals often create manual delays', 'Strong'], ['Security pattern', 'API key anomaly links to integration activity', 'Healthy'], ['Revenue attribution', 'AI usage drives enterprise expansion signals', 'Strong']];
export default function GraphInsights(){return <Card variant="elevated" style={{padding:34}}><div style={{display:"flex",justifyContent:"space-between",gap:16,marginBottom:24}}><div><Badge variant="info">Graph Insights</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:0}}>High-value AI graph discoveries</h2></div><Button variant="outline">Explore</Button></div><div style={{display:"grid",gap:12}}>{rows.map(([a,b,c])=><div key={a} style={{display:"grid",gridTemplateColumns:"1fr 170px 110px",gap:14,alignItems:"center",padding:16,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><strong style={{color:brand.colors.navy}}>{a}</strong><span style={{color:brand.colors.muted}}>{b}</span><Badge variant={c==="Risk"||c==="Conflict"?"danger":c==="Strong"||c==="Resolved"||c==="Healthy"?"success":"info"}>{c}</Badge></div>)}</div></Card>}