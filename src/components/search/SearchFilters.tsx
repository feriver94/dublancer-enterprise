import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const items=[['Projects', 'Workspace rooms, milestones, risks'],['Clients', 'CRM records, health scores, invoices'],['Payments', 'Invoices, escrow, payouts, disputes'],['Agents', 'Runs, tools, memory, approvals'],['Knowledge', 'Documents, summaries, source-grounded answers'],['Audit', 'Security, admin, workflow, integration logs']];export default function SearchFilters(){return <Card variant="elevated"><Badge variant="info">Search Filters</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Enterprise scopes</h2><div style={{display:"grid",gap:12}}>{items.map(([name,detail])=><div key={name} style={{padding:14,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><strong style={{color:brand.colors.navy}}>{name}</strong>{detail && <p style={{color:brand.colors.muted,lineHeight:1.6,margin:"8px 0 0"}}>{detail}</p>}</div>)}</div></Card>}