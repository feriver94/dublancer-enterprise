import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const items=[['Generate CRM risk report', 'Completed'],['Open AI Marketplace MVP workspace', 'Completed'],['Find unpaid invoices over $5k', 'Completed'],['Create client onboarding workflow', 'Draft'],['Review access permissions', 'Completed']];export default function RecentCommands(){return <Card variant="elevated"><Badge variant="info">Recent Commands</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Command history</h2><div style={{display:"grid",gap:12}}>{items.map(([name,detail])=><div key={name} style={{padding:14,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><strong style={{color:brand.colors.navy}}>{name}</strong>{detail && <p style={{color:brand.colors.muted,lineHeight:1.6,margin:"8px 0 0"}}>{detail}</p>}</div>)}</div></Card>}