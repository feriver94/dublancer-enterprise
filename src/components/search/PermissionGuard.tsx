import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const controls=[["RBAC checks","Every result is filtered by user permissions"],["Tenant isolation","Organization data boundaries enforced"],["Sensitive data masking","PII and financial fields protected"],["Audit logging","All search and command actions recorded"],["Command approvals","High-risk commands require human approval"]];export default function PermissionGuard(){return <Card variant="glass" style={{background:"linear-gradient(135deg,rgba(255,255,255,.96),rgba(248,250,252,.82))"}}><Badge variant="success">Permission Guard</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Secure by design</h2><div style={{display:"grid",gap:12}}>{controls.map(([n,d])=><div key={n} style={{padding:14,borderRadius:brand.radius.md,background:brand.colors.white,border:`1px solid ${brand.colors.border}`}}><strong style={{color:brand.colors.navy}}>{n}</strong><p style={{color:brand.colors.muted,lineHeight:1.6,margin:"8px 0 0"}}>{d}</p></div>)}</div></Card>}