import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const items=[['Most searches this week are related to proposals, invoices, and workspace status.', ''],['Users frequently run commands that combine CRM and payments data.', ''],['Knowledge index rebuild will improve semantic answers for long project histories.', ''],['Command approvals are correctly blocking high-risk financial actions.', '']];export default function SearchAI(){return <Card variant="elevated"><Badge variant="info">Search AI</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Search intelligence</h2><div style={{display:"grid",gap:12}}>{items.map(([name,detail])=><div key={name} style={{padding:14,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><strong style={{color:brand.colors.navy}}>{name}</strong>{detail && <p style={{color:brand.colors.muted,lineHeight:1.6,margin:"8px 0 0"}}>{detail}</p>}</div>)}</div></Card>}