import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";

const insights=["The next serious milestone is not more frontend modules: it is Prisma, PostgreSQL, Auth, API routes, and permission middleware.","Identity, organization, audit, and billing schemas should be implemented first because every other module depends on them.","AI agents need background workers, queues, tool permissions, and audit lineage before they can be called production-ready.","Event bus and webhook delivery should be designed early to avoid rebuilding automation and integration modules later.","Production claims require CI/CD, staging, logs, metrics, error handling, rate limits, and backup recovery from day one."];
export default function BackendAI(){return <Card variant="glass" style={{background:"linear-gradient(135deg,rgba(255,255,255,.96),rgba(248,250,252,.82))"}}><Badge variant="success">Backend AI Architect</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:12}}>Production architecture recommendations</h2><p style={{color:brand.colors.muted,lineHeight:1.7,marginBottom:22}}>AI reviews module dependencies, service boundaries, data models, security requirements, and deployment readiness.</p><div style={{display:"grid",gap:12}}>{insights.map(i=><div key={i} style={{padding:14,borderRadius:brand.radius.md,background:brand.colors.white,border:`1px solid ${brand.colors.border}`,color:brand.colors.text,lineHeight:1.6}}>{i}</div>)}</div></Card>}
