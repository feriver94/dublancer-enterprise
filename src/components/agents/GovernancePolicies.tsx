import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";

const policies=[["PII redaction","Enabled"],["Financial action approval","Required"],["Security action approval","Required"],["Prompt audit logging","Enabled"],["Tool permission boundaries","Enforced"],["Model fallback controls","Configured"],["Cost ceiling","Active"]];export default function GovernancePolicies(){return <Card variant="glass" style={{background:"linear-gradient(135deg,rgba(255,255,255,.96),rgba(248,250,252,.82))"}}><Badge variant="success">AI Governance</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Policy enforcement layer</h2><div style={{display:"grid",gap:12}}>{policies.map(([p,s])=><div key={p} style={{display:"flex",justifyContent:"space-between",gap:16,padding:14,borderRadius:brand.radius.md,background:brand.colors.white,border:`1px solid ${brand.colors.border}`}}><span style={{color:brand.colors.text}}>{p}</span><Badge variant="success">{s}</Badge></div>)}</div></Card>}
