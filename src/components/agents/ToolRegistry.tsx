import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";

const tools=[["CRM Tools","Read clients, update notes, create follow-ups","Ready"],["Payments Tools","Check invoice risk, escrow state, payouts","Ready"],["Workspace Tools","Summarize projects, create tasks, inspect risks","Ready"],["Notification Tools","Send alerts, route approvals, escalate events","Ready"],["Analytics Tools","Generate metrics, forecasts, executive memos","Ready"],["Admin Tools","Audit logs, policy checks, feature flags","Guarded"]];export default function ToolRegistry(){return <Card variant="elevated"><Badge variant="neutral">Tool Registry</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:22}}>Secure tool calling layer</h2><div style={{display:"grid",gap:12}}>{tools.map(([n,d,s])=><div key={n} style={{padding:14,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><div style={{display:"flex",justifyContent:"space-between",gap:14}}><strong style={{color:brand.colors.navy}}>{n}</strong><Badge variant={s==="Guarded"?"info":"success"}>{s}</Badge></div><p style={{color:brand.colors.muted,lineHeight:1.6,margin:"8px 0 0"}}>{d}</p></div>)}</div></Card>}
