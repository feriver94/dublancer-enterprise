import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";

const rows=[["Enterprise discovery call", "Summary and next steps", "Ready"], ["Security review", "Risks and decisions extracted", "Healthy"], ["Board operating review", "Executive memo prepared", "Active"], ["Client renewal meeting", "Sentiment analyzed", "Ready"], ["Sprint review", "Actions assigned", "Healthy"]];
export default function MeetingIntelligence(){return <Card variant="elevated" style={{padding:34}}><div style={{display:"flex",justifyContent:"space-between",gap:16,marginBottom:24}}><div><Badge variant="info">Meeting Intelligence</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:0}}>AI meeting summaries and decisions</h2></div><Button variant="outline">Manage</Button></div><div style={{display:"grid",gap:12}}>{rows.map(([a,b,c])=><div key={a} style={{display:"grid",gridTemplateColumns:"1fr 190px 110px",gap:14,alignItems:"center",padding:16,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><strong style={{color:brand.colors.navy}}>{a}</strong><span style={{color:brand.colors.muted}}>{b}</span><Badge variant={c==="Risk"||c==="Blocked"?"danger":c==="Active"||c==="Healthy"||c==="Ready"?"success":"info"}>{c}</Badge></div>)}</div></Card>}
