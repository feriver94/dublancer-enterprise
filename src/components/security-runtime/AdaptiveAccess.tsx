import { Card, Badge, Button } from "@/components/ui";
import { brand } from "@/constants/design";
const rows=[["Low-risk access", "Standard session", "Healthy"], ["Medium-risk access", "Step-up MFA", "Active"], ["High-risk access", "Block and review", "Blocked"], ["Finance action", "Dual approval", "Enforced"], ["Admin action", "Just-in-time access", "Ready"]];export default function AdaptiveAccess(){return <Card variant="elevated" style={{padding:34}}><div style={{display:"flex",justifyContent:"space-between",gap:16,marginBottom:24}}><div><Badge variant="info">Adaptive Access</Badge><h2 style={{color:brand.colors.navy,fontSize:brand.typography.heading.h3,fontWeight:brand.typography.weight.bold,marginTop:18,marginBottom:0}}>Risk-based enforcement</h2></div><Button variant="outline">Manage</Button></div><div style={{display:"grid",gap:12}}>{rows.map(([a,b,c])=><div key={a} style={{display:"grid",gridTemplateColumns:"1fr 220px 110px",gap:14,alignItems:"center",padding:16,borderRadius:brand.radius.md,background:brand.colors.background,border:`1px solid ${brand.colors.border}`}}><strong style={{color:brand.colors.navy}}>{a}</strong><span style={{color:brand.colors.muted}}>{b}</span><Badge variant={c==="Risk"||c==="Blocked"||c==="Expired"?"danger":c==="Active"||c==="Healthy"||c==="Ready"||c==="Enforced"?"success":"info"}>{c}</Badge></div>)}</div></Card>}
