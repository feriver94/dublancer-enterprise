# Sprint 29 uses the existing DATABASE_URL, REDIS_URL, auth, CSRF,
# realtime publisher, and notification configuration.

# Generate with:
# node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
INTERNAL_CHAT_MAINTENANCE_SECRET="replace-with-at-least-48-random-bytes"
