# Playwright uses an explicitly prestarted, disposable Dublancer environment.
# The target must have a fresh migrated PostgreSQL database, real Redis, seed
# data, and all normal application secrets. Do not put credentials in this URL.
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
